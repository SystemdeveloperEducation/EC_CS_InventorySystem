@using System.Linq;
@using static Shared.Services.Fetchdata;
@using static InvSystem.Data.Cart;
@using Newtonsoft.Json;



<div>
    @if(kundvagnLista.Count != 0) {
    <p>@kundvagnLista.Count</p>
    }
    else{
        <p>Det finns ingenting</p>
    }
</div>





@code {


    public static Cart _cart = new Cart();
    public List<Card> kundvagnLista = _cart.GetProducts();

    /* private void SaveCartToLocalStorage(Cart cart)
    {
        string json = JsonConvert.SerializeObject(cart);
        SaveCartToLocalStorage.SetItem("Cart", json);
    }*/
}